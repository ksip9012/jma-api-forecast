[project]
name = "jma-api-forecast"
version = "0.1.0"
description = "気象庁の JSON API から週間予報データを取得して CSV/JSON に保存するツール"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.5",
]
